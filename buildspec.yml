
version: 0.2

phases:
  install:
    runtime-versions:
        nodejs: 10
    commands:
      - npm install
      - npm run clientinstall
      - npm install -g migrate-mongo
  build:
    commands:
      - migrate-mongo up
artifacts:
  files:
    - '**/*'